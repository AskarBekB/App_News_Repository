<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Android_app_news" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/hex</item>
        <item name="colorPrimaryVariant">@color/hex</item>
        <item name="android:statusBarColor">@color/hex</item>
    </style>

    <style name="Theme.Android_app_news" parent="Base.Theme.Android_app_news" />

    <style name="App.Custom.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/gray_white</item>

    </style>
</resources>